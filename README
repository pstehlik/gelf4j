gelf4net created by Juan J. Chiw - Copyright 2011

based on:
gelf4j created by Philip Stehlik - Copyright 2011

See LICENSE for license details

---

Chunk message is here!

---

Now we accept loggingEvent.Properties, and we send the variables to graylog2 as additional fields

---

We have a cool project that it's a random sentence generator, to make the examples

---

gelf4net is set out to be an easy to use log4net appender to log to graylog2 via GELF messages and AMQP.

---


<?xml version="1.0"?>
<configuration>
  <configSections>
    <section name="log4net" type="log4net.Config.Log4NetConfigurationSectionHandler,Log4net"/>
  </configSections>
  
  <log4net>
    <root>
      <level value="DEBUG"/>
      <appender-ref ref="GelfFileAppender"/>
    </root>

    <appender name="GelfFileAppender" type="Esilog.Gelf4net.Appender.Gelf4NetAppender, Esilog.Gelf4net">
      <param name="GrayLogServerHostIpAddress" value="192.168.3.105" />
      <param name="Facility" value="RandomPhrases" />

      <layout type="log4net.Layout.PatternLayout">
        <param name="ConversionPattern" value="%-5p%d{yyyy-MM-dd hh:mm:ss} – %m%n"/>
      </layout>
    </appender>
    
  </log4net>

<startup><supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.0"/></startup></configuration>

---



